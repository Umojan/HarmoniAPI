# Полный поток при использовании Stripe с `PaymentIntent`

#### **Шаг 1: Backend создаёт `PaymentIntent`**

* На сервере вы делаете что-то вроде:

  ```python
  stripe.PaymentIntent.create(
      amount=2000,
      currency='usd',
      payment_method_types=['card'],  # здесь задаёте, например, только карты
      # возможно: customer=…, metadata=…, etc.
  )
  ```

  — Обратите внимание: параметр `payment_method_types` полезен — если вы его не укажете, Stripe по умолчанию применит все разрешённые платежные методы из вашей панели управления. ()
* Сервер получает ответ от Stripe с объектом `PaymentIntent`, содержащим, среди прочего, `client_secret`. ()
* Сервер возвращает `client_secret` фронтенду (но не ваш секретный ключ!), чтобы фронтенд мог дальше инициировать оплату.

#### **Шаг 2: Frontend получает `client_secret`**

* Клиент (фронтенд) получает `client_secret`, например:

  ```js
  const { client_secret: clientSecret } = await response.json();
  ```
* Затем фронтенд показывает пользователю форму оплаты (например, с помощью Stripe Payment Element или другой UI).
* Карта или другие платёжные детали **не** передаются на ваш сервер напрямую — они передаются прямо через Stripe.js к Stripe. Это важный момент ради безопасности и соответствия PCI-требованиям.

#### **Шаг 3: Frontend показывает форму и подтверждает оплату**

* На фронтенде вы вызываете что-то вроде:

  ```js
  stripe.confirmPayment({
    elements,
    clientSecret
    // возможно: , confirmParams: { return_url: "https://your.site/return" }
  });
  ```

  ()
* Если вы используете методы, которые **не требуют** редиректа (например обычная карта без дополнительной аутентификации) — пользователь может остаться на одной странице.
* Если же метод требует редиректа (например банковский перевод, 3D Secure, или иной способ с внешней аутентификацией) — то вы можете указать `return_url`, куда Stripe вернёт пользователя после завершения внешнего шага. ()

#### **Шаг 4: Stripe обрабатывает платеж**

* Stripe на своей стороне:

  * получает и токенизирует платёжную информацию (через Stripe.js)
  * обрабатывает авторизацию, возможно выполняет дополнительную аутентификацию (например 3D Secure)
  * обновляет статус `PaymentIntent` (например `requires_action` → `succeeded` или `requires_payment_method`/`canceled`) ()
* После успешного завершения может произойти редирект пользователя (если требуется вернуть его на ваш сайт) — как указано выше.
* Также Stripe отправляет webhooks на ваш backend — например событие `payment_intent.succeeded` или другое, в зависимости от метода оплаты и ситуации.

#### **Шаг 5: Backend получает Webhook**

* Ваш сервер слушает endpoint (например `/webhook/stripe`) и получает полные данные события от Stripe.
* Важные моменты:

  * Вы **должны** проверить подпись вебхука с помощью вашего `webhook_secret`, чтобы убедиться, что событие действительно от Stripe.
  * Вебхук-обработчик должен быть **идемпотентным**: если одно и то же событие придёт дважды — не должно быть повторного выполнения логики, например двойного списания, двойной отметки «оплачено», двойной отправки письма и т.д.
* После получения события (скажем, `payment_intent.succeeded`) вы обновляете вашу базу данных: помечаете заказ как оплаченный, активируете услугу и т.п.

#### **Шаг 6: Редирект / отображение статуса пользователю**

* Если вы контролируете фронтенд страницу после оплаты:

  * Если вы использовали редирект-сценарий (например метод оплаты требовал внешней аутентификации) — пользователь будет возвращён по `return_url` либо (`success_url` в случае использования Checkout) с параметром (например `session_id={CHECKOUT_SESSION_ID}`). ()
  * Если вы использовали интеграцию без обязательного редиректа — пользователь может остаться на странице, и вы можете показать ему статус оплаты прямо после `confirmPayment`.
* Чтобы пользователь мог **увидеть статус оплаты**:

  * На вашем success-странице вы можете считать параметр `session_id` или `payment_intent` из URL и своим API-запросом получить детали платежа/сессии и показать пользователю: «Ваш платёж прошёл успешно», «Ожидание подтверждения», «Произошла ошибка». ()
  * Но **важно**: не полагайтесь только на фронтенд и редирект, чтобы считать, что заказ оплачен. Кто-то может закрыть браузер до редиректа. Лучшей практикой является полагаться на вебхук-событие для финального подтверждения оплаты. ()

---

### 🔍 Ключевые нюансы, не забываем

* **`payment_method_types`**: Указав его явно (например `['card']`), вы ограничиваете способы оплаты. Если не указано — Stripe будет использовать те методы, которые включены в вашей Stripe-панели. ()
* **Идемпотентность вебхуков**: важно, иначе двойные записи/действия могут привести к ошибкам в бизнес-логике — например, заказ может быть выполнен дважды.
* **Редиректы**:

  * Если вы используете обычный `PaymentIntent + Elements`, без внешних редирект-методов — редиректа может не быть, и вы остаётесь на той же странице.
  * Если используется метод с редиректом или вы используете Stripe Checkout (хостинг-страница Stripe) — то задаётся `success_url` (и `cancel_url`) и возможно `return_url`. ()
* **Статус оплаты для пользователя**: Лучше показывать статус на фронтенде *и* иметь сервер-логика, подтверждённую вебхуками для окончательной обработки.
* Также: если пользователь не переходит на success-страницу (закрыл браузер, интернет пропал) — ваш сервер (вебхук) всё равно получит события, и заказ будет выполнен корректно. Поэтому фронтенд-редирект не заменяет вебхук-логику. ()
